services:
  backend:
    build: ./fluxtube-backend
    ports:
      - "5000:5000"
    env_file: ./fluxtube-backend/.env

  frontend:
    build: ./fluxtube-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:5000
    depends_on:
      - backend